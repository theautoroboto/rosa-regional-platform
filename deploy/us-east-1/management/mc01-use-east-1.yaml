account_id: "REMOVED"   # The AWS Account ID of the Regional Account -- SANDBOX04
region: "us-east-1"          # The region to deploy the Regional Cluster and Pipeline into
alias: "management01-us-east-1"  # Unique alias for this regional environment

# Terraform variables for infrastructure tagging
terraform_vars:
  app_code: "infra"
  service_phase: "dev"
  cost_center: "000"
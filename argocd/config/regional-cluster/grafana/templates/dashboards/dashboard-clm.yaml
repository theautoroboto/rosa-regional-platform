apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-clm
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
  annotations:
    k8s-sidecar-target-directory: "Infrastructure"
data:
  clm.json: {{ .Files.Get "dashboards/infrastructure/clm.json" | quote }}

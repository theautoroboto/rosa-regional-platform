apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-eks-standards
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
  annotations:
    k8s-sidecar-target-directory: "Infrastructure"
data:
  eks-standards.json: {{ .Files.Get "dashboards/infrastructure/eks-standards.json" | quote }}

apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-eks-control-plane
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
  annotations:
    k8s-sidecar-target-directory: "Infrastructure"
data:
  eks-control-plane.json: {{ .Files.Get "dashboards/infrastructure/eks-control-plane.json" | quote }}
